package com.profile.developer.impl;

import java.util.Date;

import com.profile.developer.bean.User;
import com.profile.developer.service.UserInterface;

public class UserRepository {

	/*
	 * int count = 0; //private String generatedUserId = "Sandeep"+count; private
	 * UserInterface userInterface;
	 * 
	 * public UserRepository(UserInterface userInterface) { super();
	 * this.userInterface = userInterface; }
	 * 
	 * 
	 * public int insertUser(User users) {
	 * 
	 * String userNm = users.getUserName(); Date dob = users.getDateOfBirth();
	 * 
	 * userInterface.save(users); return count;
	 * 
	 * }
	 */
	
	
	
}
